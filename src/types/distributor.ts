export type DistributorDetail = {
  distNum: number;
  name: string;
};

export type DistributorListEntry = {
  distNum: number;
  name: string;
}

export type DistributorCreation = {
  name: string;
}