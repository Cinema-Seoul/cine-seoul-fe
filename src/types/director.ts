export type DirectorDetail = {
  dirNum: number;
  imgUrl?: string;
  name: string;
};

export type DirectorListEntry = {
  dirNum: number;
  imgUrl?: string;
  name: string;
};

export type DirectorCreation = {
  imgUrl?: string;
  name: string;
};
